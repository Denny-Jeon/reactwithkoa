import React from "react";
import { Header, ContentMain, Footer } from "../components";

const Root = () => (
  <div>
    <Header />
    <ContentMain />
    <Footer />
  </div>
);

export default Root;
